services:
  backend:
    build: ./FastAPI-backend
    ports: ["8000:8000"]
    env_file: .env
  frontend:
    build: ./Frontend
    ports: ["3000:3000"]
  qdrant:
    image: qdrant/qdrant
    ports: ["6333:6333"]
  postgres:
    image: postgres:15
    ports: ["5432:5432"]
